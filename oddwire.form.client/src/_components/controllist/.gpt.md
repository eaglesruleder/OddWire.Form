# Doc — Dynamic Form

**Scope:** Feature
**Domain:** oddform
**Status:** Active

---

## Summary

**Goal:** Render a JSON form definition overlaid by a live instance array, dispatching each control on its resolved `type` and mutating the instance by `param`.

**Lifecycle:**
1. FormPage asks `FormContext.getForm(id)` for the definition (loading shell until resolved)
2. `InstanceEntity` asks `InstanceContext.getInstance(id)` for the live document
3. Pass `form.controls` + the `instance` (a `FormActiveInstance`) to `ControlList`
4. ControlList hands each form control plus the instance to a `ControlItem`
5. ControlItem calls `instance.resolve(control)` (merge boundary), switches on resolved `type` (unknown → `ControlBase` error box)
6. User edits → `onChange(value, param, key)` → `InstanceEntity` `instance.setValue(param, {...get(param), [key]: value})` → re-render + `InstanceContext.set` (persist)

**Rules:**
- Type dispatch + merge live only in `ControlItem`; `ControlList` stays type-agnostic
- Unknown `type` renders a `ControlBase` error box, never crashes
- Hidden controls render nothing (each leaf / `ControlBase` gates `hidden`)
- Instance overlays by `param`; instance props override form props; duplicate params resolve last-wins (dev warn)
- Instance stays an array; edits are immutable array replacements
- Contexts are thin data APIs; the live instance is owned by `InstanceEntity`, persisted via localforage (IndexedDB)

**State:** `Stored: form (FormDefinition|null) in FormPage, active instance in InstanceEntity — Persisted: instances via localforage on every edit`

---

## Structure

`FormPage` → `src/form/` · `FormContext`/`InstanceContext` + `FormDefinition`/`FormInstance`/`ControlInstance` (`types.ts`) + `data/forms/*.json` + `data/instances/*.json` + barrel → `src/_context/` · `ControlList` + `ControlItem` + `controls/` → `src/_components/controllist/` (this folder). `ControlDef` lives in `controls/controlTypes.ts`, re-exported from the controllist barrel. Control leaves + their contracts map is `controls.gpt.md` in `controls/`.

---

## Implementation

### FormContext / InstanceContext
Thin data APIs (value = the `persistent` store, supplied by `ContextsProvider`):
- `FormContext.getForm(id): Promise<FormDefinition>`
- `InstanceContext.getInstance(id): Promise<FormActiveInstance>` and `set(instance)` — the autosave write-back

### persistentStore
`PersistentStore` singleton behind localforage (IndexedDB, localStorage fallback). `initialise()` loads `forms`/`instances`, seeding from bundled JSON on first run only; `getForm`/`getInstance` resolve by id; `set` upserts an instance by `instanceId` and persists the array. Structured/binary values store natively (ready for PDF/image blobs).

### FormActiveInstance
The live instance as a behaviour-carrying object — `implements FormInstance` (the sparse overlay) and owns the operations over it:
- `get(param)` — last-wins match; dev `console.warn` on duplicate `param`
- `resolve(control)` — `{ ...control, ...get(control.param) }`, cast once at the merge boundary
- `setValue(param, control)` — upsert the whole merged control patch by `param`; returns a new `FormActiveInstance` (immutable)

### InstanceEntity
Render-prop microcontext owning the active document over the `InstanceContext` API. Loads the instance (`getInstance`), yields `children(instance, onChange)`. `onChange(value, param, key='value')` → `instance.setValue(param, {...get(param), [key]: value})` → local re-render + `InstanceContext.set` (persist). Not a React context.

### FormPage
Loads the definition via `FormContext.getForm` (loading shell until resolved), then renders `StripLayout > Form > InstanceEntity`, whose render prop feeds `form.controls` + the live `instance` + `onChange` into `ControlList`.

### ControlList
Type-agnostic iteration. Hands each form `control` plus the whole `instance` to a `ControlItem`. No merge, no type knowledge.

### ControlItem
The resolve + dispatch boundary. `instance.resolve(control)` merges form + instance, then `switch(resolved.type)` spreads into the matching leaf (interactive leaves also get `onChange`); `default` → bold dark-red `ControlBase label="Error"` box (`className="error" labelClassName="bold"`) reading `Unknown control type: {type}`.

### StripLayout
Headered tablet-portrait shell — masthead title + padded `main`. Strip width and borders live on `#root` (layout.css); this only supplies the header band and body slot.

---

_Not built yet: real context folder-scan + landing navigation (Stage 4), MVP+ layout/DB controls._
